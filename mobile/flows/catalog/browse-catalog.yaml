# Selectors verified against fragment_product_catalog.xml.
# The catalog is the app's default screen — no login required to browse.
appId: com.saucelabs.mydemoapp.android
---
- launchApp:
    clearState: true
- assertVisible: "Products"
- scroll
- assertVisible: "Sauce Labs Backpack"
